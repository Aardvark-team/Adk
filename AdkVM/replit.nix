{ pkgs }: {
  deps = [
    pkgs.gcc
  ];
}
# want to migrate this to a nix repl?
# created the repl @programit/Adk
# Im going to add AdkC and AdkASM into one repl if we switch to nix. Easier testing
#OK